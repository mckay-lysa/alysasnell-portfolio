# McKay is the required reviewer on every pull request.
#
# The branch ruleset on `main` has "Require review from Code Owners" turned on,
# so GitHub automatically requests his review and blocks the merge until he
# approves. This works on a free account because the repo is public.

* @mckayqsnell
